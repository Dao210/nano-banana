# 2026-08-16 工作日志

## 域名全局替换：nanobanana.fans → nano.theskills.ai

将整个项目的域名从 `nanobanana.fans` 迁移到 `nano.theskills.ai`。

### 变更范围
- **Header Logo 文本**：`components/header.tsx` 第 62 行 `NanoBanana.Fans` → `Nano.TheSkills.AI`
- **全量 URL 替换**（30 个文件）：涵盖 SEO metadata、sitemap、canonical、schema、面包屑、邮件地址等

### 替换规则（两步 sed）
1. `www.nanobanana.fans` → `nano.theskills.ai`（去 www）
2. `nanobanana.fans` → `nano.theskills.ai`（覆盖子域名：cdn./api. 自动迁移为 cdn.nano.theskills.ai / api.nano.theskills.ai）

### 关键文件验证
- `package.json` name → `nano.theskills.ai`
- `next.config.js` hostname → `nano.theskills.ai`、`cdn.nano.theskills.ai`
- `app/layout.tsx` metadataBase / canonical / twitter:domain → `nano.theskills.ai`
- `app/contact/page.tsx` 邮箱 → `hello@nano.theskills.ai`、`support@nano.theskills.ai`
- `app/figure-generate/page.tsx` API → `api.nano.theskills.ai`

### 注意
文档文件（docs/、README.md、SEO-OPTIMIZATION-GUIDE.md、IFLOW.md、tasks/）中的域名也已一并替换，部分为历史记录性文档，内容描述可能仍指向旧项目背景。

### 推送
- commit: `refactor: 域名迁移 nanobanana.fans → nano.theskills.ai`
- push 到 `origin/main`（82eb2fc..b3131a4）成功，共 40 个对象（47 个变更）
- 排除 `.DS_Store`（已跟踪但属系统文件），`.workbuddy/` 未跟踪未提交
